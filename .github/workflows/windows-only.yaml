name: Build MLC LLM Runtime (Windows)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build mlc_llm from source
    runs-on: windows-latest

    defaults:
      run:
        shell: bash -l {0}

    steps:

    # 1️⃣ Checkout repository + submodules
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    # 2️⃣ Setup Conda (same as Linux)
    - name: Setup Miniconda
      uses: conda-incubator/setup-miniconda@v3
      with:
        auto-update-conda: true
        auto-activate-base: false
        activate-environment: mlc-chat-venv
        python-version: 3.13

    # 3️⃣ Create fresh build environment
    - name: Create build environment
      run: |
        conda create -y -n mlc-chat-venv -c conda-forge \
          "cmake>=3.24" \
          rust \
          git \
          python=3.13

    # 4️⃣ Generate CMake config (non-interactive)
    - name: Generate CMake config
      run: |
        conda activate mlc-chat-venv

        mkdir -p build
        cd build

        printf "\nn\nn\nn\nn\nn\n" | python ../cmake/gen_cmake_config.py

    # 5️⃣ Build mlc_llm runtime (parallel for speed)
    - name: Build mlc_llm runtime
      run: |
        conda activate mlc-chat-venv
        cd build

        cmake ..
        cmake --build . --parallel

    # 6️⃣ Build TVM runtime libraries
    - name: Build TVM runtime
      run: |
        conda activate mlc-chat-venv
        cd 3rdparty/tvm

        mkdir -p build
        cd build

        cmake ..
        cmake --build . --parallel

    # 7️⃣ Install TVM Python bindings
    - name: Install TVM Python bindings
      run: |
        conda activate mlc-chat-venv
        cd 3rdparty/tvm/python
        pip install -e .

    # 8️⃣ Install mlc_llm Python package
    - name: Install mlc_llm Python package
      run: |
        conda activate mlc-chat-venv
        cd python
        pip install -e .

    # 9️⃣ Install runtime Python dependencies
    - name: Install runtime Python dependencies
      run: |
        conda activate mlc-chat-venv
        pip install psutil numpy decorator attrs cloudpickle

    # 🔟 Export DLL paths (Windows equivalent of LD_LIBRARY_PATH)
    - name: Export library paths
      run: |
        echo "PATH=$PATH:$(pwd)/build:$(pwd)/3rdparty/tvm/build" >> $GITHUB_ENV

    # 1️⃣1️⃣ Validate installation
    - name: Validate build
      run: |
        conda activate mlc-chat-venv

        echo "=== Build artifacts ==="
        ls -l ./build/ | grep mlc || true
        ls -l ./3rdparty/tvm/build/ | grep tvm || true

        echo "=== CLI test ==="
        mlc_llm chat -h

        echo "=== Python import test ==="
        python -c "import mlc_llm; print(mlc_llm)"

    # 1️⃣2️⃣ Upload artifacts
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mlc-llm-build-windows
        path: |
          build/
          3rdparty/tvm/build/
